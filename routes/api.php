<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| No API endpoints are exposed by the vendor portal. (The former QS Studio
| matching API was removed along with its tables — the portal is now the
| vendor-facing UI flow only.)
|
*/
